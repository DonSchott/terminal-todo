# terminal-todo

A simple todo manager in a single file.

## Installation
```bash
pip install .
```

## Usage
```bash
todo add "write report"
todo list
```

## Implementation
Everything lives in `todo.py` (~60 lines).
