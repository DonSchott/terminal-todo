[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "terminal-todo"
version = "0.1.0"
description = "A fast, minimal CLI-based todo manager"
requires-python = ">=3.11"
dependencies = [
    "click>=8.1.0",
]

[project.scripts]
todo = "todo:cli"
